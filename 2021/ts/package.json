{
  "license": "UNLICENSED",
  "scripts": {
    "build": "for dir in day**/ts/; do cd \"$(pwd)/$dir\"; tsc; cd ../../; done",
    "start": "for dir in day**/ts/; do cd \"$(pwd)/$dir\"; node dist/day**.js; cd ../../; done",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "@types/jest": "^27.4.0",
    "@types/jest-cli": "^24.3.0",
    "@types/node": "^16.11.11",
    "jest": "^27.5.1",
    "ts-jest": "^27.1.3"
  },
  "dependencies": {
    "jest-cli": "^27.5.1",
    "ts-node": "^10.5.0",
    "typescript": "^4.5.5"
  }
}
